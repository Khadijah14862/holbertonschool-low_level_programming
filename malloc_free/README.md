# malloc_free

This directory contains C functions that use dynamic memory allocation with malloc and free.

## Task 0

`create_array` allocates an array of characters and initializes every element with a specified character.

Prototype:

char *create_array(unsigned int size, char c);

The function returns NULL when size is zero or when memory allocation fails.
